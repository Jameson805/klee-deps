[[benchmarks]]
library = "libgcrypt"
code_path = "benchmarks/libgcrypt-and-libgpg-error"
tools = ["klee_cf", "klee_eager", "klee_self_comp", "binsec", "abacus"]

[benchmarks.runner_profiles.default]
config = "configs/runner/modexp_runner_config.toml"
preset = "size_{sym_size}"

[benchmarks.runner_profiles.rsa_private]
config = "configs/runner/libgcrypt_rsa_private_runner_config.toml"
preset = "size_128"

[benchmarks.runner_profiles.rsa_padding]
config = "configs/runner/libgcrypt_rsa_padding_runner_config.toml"
preset = "size_{sym_size}"

[benchmarks.runner_profiles.aes]
config = "configs/runner/symmetric_aes_runner_config.toml"
preset = "default"

[benchmarks.runner_profiles.des]
config = "configs/runner/symmetric_des_runner_config.toml"
preset = "default"

[benchmarks.runner_profiles.ecdsa]
config = "configs/runner/ecdsa_sign_runner_config.toml"
preset = "default"

[benchmarks.build]
preset = "size_{sym_size}"
common_flags = ["-g", "-O0"]
include_dirs = ["{repo_root}/include", "generated/{target_output}", "{code_path}/build/include"]
link_libraries = ["{code_path}/build/lib/libgcrypt.a", "{code_path}/build/lib/libgpg-error.a"]
variables = { libgcrypt_host = "", libgcrypt_dir = "libgcrypt-1.10.1" }
prepare_commands = ["mkdir -p generated/modexp"]
build_dep_commands = [
	"cd libgpg-error-1.44 && ./configure CC={cc} CFLAGS=\"{dep_cflags}\" LDFLAGS=\"{dep_ldflags}\" --enable-static --disable-shared --disable-doc --prefix=\"{install_root}\" {libgcrypt_host} && make clean && make -j{make_jobs} && make install",
	"cd {libgcrypt_dir} && ./configure CC={cc} CFLAGS=\"{dep_cflags} -DNO_ASM\" LDFLAGS=\"{dep_ldflags}\" --enable-static --disable-shared --disable-asm --disable-doc --with-libgpg-error-prefix=\"{install_root}\" --with-sysroot=\"{install_root}\" --prefix=\"{install_root}\" {libgcrypt_host} && make clean && make -j{make_jobs} && make install",
]

[benchmarks.build.tools.abacus]
concrete_pubs = false
variables = { libgcrypt_host = "--host=i686-pc-linux-gnu" }

[benchmarks.tool_defaults.klee]
code_path = "benchmarks/libgcrypt-and-libgpg-error/libgcrypt-1.10.1/mpi"
secret_inputs = ["exp"]
public_inputs = ["base", "mod"]
mod_exp_extra_args = ["--filename", "mpi-pow.c", "--lines", "404:771"]

[benchmarks.tool_defaults.klee.preprocess_profiles.lim_loop_break]
arguments = ["-break", "-o", "{output}", "{input}"]

[benchmarks.variants.default]

[[benchmarks.targets]]
target = "modexp"
runner_profile = "default"
secret_layout = "exp:{sym_size}"
secret_inputs = ["exp:{sym_size}:exp_buf"]
public_layout = "base:{sym_size},mod:{sym_size}"
public_inputs = ["base:{sym_size}:base_buf", "mod:{sym_size}:mod_buf"]
build_sources = ["modexp_wrapper.c"]

[[benchmarks.configs]]
config = "fix_pub"
public_mode = "fix_pub"
runner_profile = "default"

[[benchmarks.configs]]
config = "var_pub"
public_mode = "var_pub"
tools = ["klee_cf", "klee_eager", "klee_self_comp", "binsec"]
use_public_inputs = true

[[benchmarks.configs]]
config = "var_pub_lim_loop_break"
public_mode = "var_pub"
tools = ["klee_cf", "klee_eager"]
preprocess = "lim_loop_break"
use_public_inputs = true

[benchmarks.variants.sliced]
tools = ["klee_cf"]

[benchmarks.variants.rsa_stages.build]
preset = "size_{sym_size}"
common_flags = ["-g", "-O0"]
include_dirs = ["{repo_root}/include", "generated/{target_output}", "{code_path}/build/include"]
link_libraries = ["{code_path}/build/lib/libgcrypt.a", "{code_path}/build/lib/libgpg-error.a"]
variables = { libgcrypt_host = "", libgcrypt_dir = "libgcrypt-1.10.1" }
build_dep_commands = [
	"cd libgpg-error-1.44 && ./configure CC={cc} CFLAGS=\"{dep_cflags}\" LDFLAGS=\"{dep_ldflags}\" --enable-static --disable-shared --disable-doc --prefix=\"{install_root}\" {libgcrypt_host} && make clean && make -j{make_jobs} && make install",
	"cd {libgcrypt_dir} && ./configure CC={cc} CFLAGS=\"{dep_cflags} -DNO_ASM\" LDFLAGS=\"{dep_ldflags}\" --enable-static --disable-shared --disable-asm --disable-doc --with-libgpg-error-prefix=\"{install_root}\" --with-sysroot=\"{install_root}\" --prefix=\"{install_root}\" {libgcrypt_host} && make clean && make -j{make_jobs} && make install",
]

[benchmarks.variants.rsa_stages.build.tools.klee]
link_flags = ["-Wl,--wrap=_gcry_mpi_randomize"]

[benchmarks.variants.rsa_stages.build.tools.binsec]
link_flags = ["-Wl,--wrap=_gcry_mpi_randomize"]

[benchmarks.variants.rsa_stages.build.tools.abacus]
link_flags = ["-Wl,--wrap=_gcry_mpi_randomize"]
concrete_pubs = false
variables = { libgcrypt_host = "--host=i686-pc-linux-gnu" }

[benchmarks.variants.rsa_stages.tool_defaults.klee]
code_path = "benchmarks/libgcrypt-and-libgpg-error/libgcrypt-1.10.1/cipher"
extra_args = ["--ctchecker-prefix", "cipher"]

[benchmarks.variants.aes_des.build]
preset = "default"
common_flags = ["-g", "-O0"]
include_dirs = ["{repo_root}/include", "generated/{target_output}", "{code_path}/build/include"]
link_libraries = ["{code_path}/build/lib/libgcrypt.a", "{code_path}/build/lib/libgpg-error.a"]
variables = { libgcrypt_host = "", libgcrypt_dir = "libgcrypt-1.10.1" }
build_dep_commands = [
	"cd libgpg-error-1.44 && ./configure CC={cc} CFLAGS=\"{dep_cflags}\" LDFLAGS=\"{dep_ldflags}\" --enable-static --disable-shared --disable-doc --prefix=\"{install_root}\" {libgcrypt_host} && make clean && make -j{make_jobs} && make install",
	"cd {libgcrypt_dir} && ./configure CC={cc} CFLAGS=\"{dep_cflags} -DNO_ASM\" LDFLAGS=\"{dep_ldflags}\" --enable-static --disable-shared --disable-asm --disable-doc --with-libgpg-error-prefix=\"{install_root}\" --with-sysroot=\"{install_root}\" --prefix=\"{install_root}\" {libgcrypt_host} && make clean && make -j{make_jobs} && make install",
]

[benchmarks.variants.aes_des.build.tools.abacus]
concrete_pubs = true
variables = { libgcrypt_host = "--host=i686-pc-linux-gnu" }

[benchmarks.variants.aes_des.tool_defaults.klee]
code_path = "benchmarks/libgcrypt-and-libgpg-error/libgcrypt-1.10.1/cipher"
extra_args = ["--ctchecker-prefix", "cipher"]
secret_inputs = ["key", "data"]

[benchmarks.variants.ecdsa.build]
preset = "default"
common_flags = ["-g", "-O0"]
include_dirs = ["{repo_root}/include", "generated/{target_output}", "{code_path}/build/include"]
link_libraries = ["{code_path}/build/lib/libgcrypt.a", "{code_path}/build/lib/libgpg-error.a"]
variables = { libgcrypt_host = "", libgcrypt_dir = "libgcrypt-1.10.1" }
build_dep_commands = [
	"cd libgpg-error-1.44 && ./configure CC={cc} CFLAGS=\"{dep_cflags}\" LDFLAGS=\"{dep_ldflags}\" --enable-static --disable-shared --disable-doc --prefix=\"{install_root}\" {libgcrypt_host} && make clean && make -j{make_jobs} && make install",
	"cd {libgcrypt_dir} && ./configure CC={cc} CFLAGS=\"{dep_cflags} -DNO_ASM\" LDFLAGS=\"{dep_ldflags}\" --enable-static --disable-shared --disable-asm --disable-doc --with-libgpg-error-prefix=\"{install_root}\" --with-sysroot=\"{install_root}\" --prefix=\"{install_root}\" {libgcrypt_host} && make clean && make -j{make_jobs} && make install",
]

[benchmarks.variants.ecdsa.build.tools.abacus]
concrete_pubs = true
variables = { libgcrypt_host = "--host=i686-pc-linux-gnu" }

[benchmarks.variants.ecdsa.tool_defaults.klee]
code_path = "benchmarks/libgcrypt-and-libgpg-error/libgcrypt-1.10.1/cipher"
extra_args = ["--ctchecker-prefix", "cipher"]
secret_inputs = ["private_key"]
public_inputs = ["digest"]

[[benchmarks.variants.rsa_stages.targets]]
target = "gcry_pk_decrypt_pkcs1"
runner_profile = "rsa_private"
secret_layout = "d:128"
secret_inputs = ["d:128:d_buf"]
public_layout = "ciphertext:128"
public_inputs = ["ciphertext:128:ciphertext_buf"]
build_sources = ["gcry_pk_decrypt_wrapper.c", "libgcrypt_randomize_wrapper.c"]
defines = ["LIBGCRYPT_RSA_DECRYPT_PKCS1"]

[[benchmarks.variants.rsa_stages.targets]]
target = "gcry_pk_decrypt_oaep"
runner_profile = "rsa_private"
secret_layout = "d:128"
secret_inputs = ["d:128:d_buf"]
public_layout = "ciphertext:128"
public_inputs = ["ciphertext:128:ciphertext_buf"]
build_sources = ["gcry_pk_decrypt_wrapper.c", "libgcrypt_randomize_wrapper.c"]
defines = ["LIBGCRYPT_RSA_DECRYPT_OAEP"]

[[benchmarks.variants.rsa_stages.targets]]
target = "gcry_pk_decrypt_raw"
runner_profile = "rsa_private"
secret_layout = "d:128"
secret_inputs = ["d:128:d_buf"]
public_layout = "ciphertext:128"
public_inputs = ["ciphertext:128:ciphertext_buf"]
build_sources = ["gcry_pk_decrypt_wrapper.c", "libgcrypt_randomize_wrapper.c"]
defines = ["LIBGCRYPT_RSA_DECRYPT_RAW"]

[[benchmarks.variants.rsa_stages.targets]]
target = "rsa_pkcs1_decode_for_enc"
runner_profile = "rsa_padding"
secret_layout = "encoded:128"
secret_inputs = ["encoded:128:encoded_buf"]
build_sources = ["rsa_padding_decode_wrapper.c", "libgcrypt_randomize_wrapper.c"]
defines = ["LIBGCRYPT_RSA_PKCS1_DECODE_FOR_ENC"]

[[benchmarks.variants.rsa_stages.targets]]
target = "rsa_oaep_decode"
runner_profile = "rsa_padding"
secret_layout = "encoded:128"
secret_inputs = ["encoded:128:encoded_buf"]
build_sources = ["rsa_padding_decode_wrapper.c", "libgcrypt_randomize_wrapper.c"]
defines = ["LIBGCRYPT_RSA_OAEP_DECODE"]

[[benchmarks.variants.rsa_stages.configs]]
config = "fix_pub"
public_mode = "fix_pub"

[[benchmarks.variants.rsa_stages.configs]]
config = "var_pub"
public_mode = "var_pub"
tools = ["klee_cf", "klee_eager", "binsec"]
use_public_inputs = true

[benchmarks.variants.sliced.build]
preset = "size_{sym_size}"
common_flags = ["-g", "-O0"]
include_dirs = ["{repo_root}/include", "generated/{target_output}", "{code_path}/build/include"]
link_libraries = ["{code_path}/build/lib/libgcrypt.a", "{code_path}/build/lib/libgpg-error.a"]
variables = { libgcrypt_host = "", libgcrypt_dir = "libgcrypt-1.10.1-sliced" }
prepare_commands = ["mkdir -p generated/modexp_sliced"]
build_dep_commands = [
	"cd libgpg-error-1.44 && ./configure CC={cc} CFLAGS=\"{dep_cflags}\" LDFLAGS=\"{dep_ldflags}\" --enable-static --disable-shared --disable-doc --prefix=\"{install_root}\" {libgcrypt_host} && make clean && make -j{make_jobs} && make install",
	"cd libgcrypt-1.10.1-sliced && ./configure CC={cc} CFLAGS=\"{dep_cflags} -DNO_ASM\" LDFLAGS=\"{dep_ldflags}\" --enable-static --disable-shared --disable-asm --disable-doc --with-libgpg-error-prefix=\"{install_root}\" --with-sysroot=\"{install_root}\" --prefix=\"{install_root}\" {libgcrypt_host} && make clean && make -j{make_jobs} && make install",
]

[benchmarks.variants.sliced.build.tools.abacus]
concrete_pubs = false
variables = { libgcrypt_host = "--host=i686-pc-linux-gnu" }

[benchmarks.variants.sliced.tool_defaults.klee]
code_path = "benchmarks/libgcrypt-and-libgpg-error/libgcrypt-1.10.1-sliced/mpi"
secret_inputs = ["exp"]
public_inputs = ["base", "mod"]
mod_exp_extra_args = ["--filename", "mpi-pow.c", "--lines", "404:771"]

[benchmarks.variants.sliced.tool_defaults.klee.preprocess_profiles.lim_loop_break]
arguments = ["-break", "-o", "{output}", "{input}"]

[[benchmarks.variants.sliced.targets]]
target = "modexp"
runner_profile = "default"
secret_layout = "exp:{sym_size}"
secret_inputs = ["exp:{sym_size}:exp_buf"]
public_layout = "base:{sym_size},mod:{sym_size}"
public_inputs = ["base:{sym_size}:base_buf", "mod:{sym_size}:mod_buf"]
build_sources = ["modexp_wrapper.c", "powm_sliced.c"]
defines = ["USE_SLICED"]

[[benchmarks.variants.sliced.configs]]
config = "fix_pub"
public_mode = "fix_pub"

[[benchmarks.variants.sliced.configs]]
config = "var_pub"
public_mode = "var_pub"
use_public_inputs = true

[[benchmarks.variants.sliced.configs]]
config = "var_pub_lim_loop_break"
public_mode = "var_pub"
preprocess = "lim_loop_break"
use_public_inputs = true

[[benchmarks.variants.aes_des.targets]]
target = "aes_encrypt"
runner_profile = "aes"
secret_layout = "key:16,data:16"
secret_inputs = ["key:16:key_buf", "data:16:data_buf"]
klee_secret_inputs = ["key", "data"]
build_sources = ["cipher_encrypt_wrapper.c"]
defines = ["LIBGCRYPT_CIPHER_AES128"]

[[benchmarks.variants.aes_des.targets]]
target = "des_encrypt"
runner_profile = "des"
secret_layout = "key:8,data:8"
secret_inputs = ["key:8:key_buf", "data:8:data_buf"]
klee_secret_inputs = ["key", "data"]
build_sources = ["cipher_encrypt_wrapper.c"]
defines = ["LIBGCRYPT_CIPHER_DES"]

[[benchmarks.variants.aes_des.configs]]
config = "fix_pub"
public_mode = "fix_pub"

[[benchmarks.variants.aes_des.configs]]
config = "var_pub"
public_mode = "var_pub"
tools = ["klee_cf", "klee_eager", "klee_self_comp", "binsec"]

[[benchmarks.variants.ecdsa.targets]]
target = "ecdsa_sign"
runner_profile = "ecdsa"
secret_layout = "private_key:32"
secret_inputs = ["private_key:32:private_key_buf"]
public_layout = "digest:32"
public_inputs = ["digest:32:digest_buf"]
klee_secret_inputs = ["private_key"]
klee_public_inputs = ["digest"]
build_sources = ["ecdsa_sign_wrapper.c"]

[[benchmarks.variants.ecdsa.configs]]
config = "fix_pub"
public_mode = "fix_pub"

[[benchmarks.variants.ecdsa.configs]]
config = "var_pub"
public_mode = "var_pub"
tools = ["klee_cf", "klee_eager", "klee_self_comp", "binsec"]
use_public_inputs = true
